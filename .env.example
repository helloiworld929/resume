VITE_BASE_PATH=/
# Local defaults. Replace these complete URLs when the projects are deployed publicly.
# Keep the port in the value when a public service uses a non-standard port.
VITE_YUYUE_DEMO_URL=http://localhost:8080
VITE_NOTIFYFLOW_DEMO_URL=http://localhost:8080/tasks
